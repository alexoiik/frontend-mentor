# Browser Extensions Manager UI

A modern, responsive UI for managing browser extensions.
This project features a clean interface, tab-based filtering (All → Active → Inactive), dark mode support, interactive toggle switches, and a flexible layout for desktop and mobile.

## 🚀 Features

- View all extensions in a clean grid layout
- Filter extensions by All, Active, or Inactive
- Functional toggle switches for enabling/disabling extensions
- Remove button for each extension (UI only)
- Dark mode with smooth transitions
- Fully responsive (desktop & mobile optimized)
- Dynamic rendering based on data.json
- Auto-mapped extension logos for cleaner imports

## 🛠 Technologies Used

- React
- TailwindCSS
- JavaScript (ES6+)
- Responsive & mobile-first workflow

## 🔧 Installation & Setup

### Clone the repo

`git clone https://github.com/your-username/browser-extensions-manager.git`

### Navigate to the project

`cd browser-extensions-manager`

### Install dependencies

`npm install`

### Run the project

`npm run start`

## 💡 What I Learned

- Managing UI state for toggles and filters
- Creating dynamic components from JSON data
- Dark mode theming in React
- Mobile-first responsive layout techniques
- Building UI components (All / Active / Inactive tabs)
